# ♿ Реализация доступности для людей с ограниченными возможностями зрения

## ✅ Что реализовано:

### 1. Виджеты доступности

#### AccessibleText
- Автоматическая проверка контрастности (WCAG AA: минимум 4.5:1)
- Правильные семантические метки для screen readers
- Поддержка крупного текста для слабовидящих

#### AccessibleButton
- Семантические метки для кнопок
- Поддержка подсказок (hints)
- Правильное объявление состояния (enabled/disabled)

#### AccessibleTextField
- Метки и подсказки для полей ввода
- Улучшенная контрастность границ
- Правильная семантика для screen readers

#### AccessibleCard
- Семантические метки для карточек
- Поддержка интерактивных карточек с навигацией
- Автоматическая структуризация контента

#### AccessibleScaffold
- Полная семантика для экранов
- Правильная навигация с клавиатуры
- Поддержка AppBar, Drawer, FAB

### 2. Текущая поддержка в экранах:

#### ✅ Реализовано:
- `Phq9TestScreen` - полная поддержка Semantics
- `Gad7TestScreen` - полная поддержка Semantics
- `TrafficLightTestScreen` - полная поддержка Semantics
- `TeenHomeScreen` - частичная поддержка
- Использование `AccessibleText` в тестовых экранах

#### ⚠️ Требует улучшения:
- `LoginScreen` - нужно добавить Semantics
- `RegisterScreen` - нужно добавить Semantics
- `ParentHomeScreen` - нужно добавить Semantics
- `AgeGateScreen` - нужно добавить Semantics
- `ParentalConsentScreen` - нужно добавить Semantics

### 3. Требования WCAG 2.1 AA:

#### ✅ Контрастность:
- Текст: минимум 4.5:1 (реализовано в `AccessibleText`)
- Крупный текст: минимум 3:1
- Интерактивные элементы: четкие границы

#### ✅ Семантика:
- Все интерактивные элементы имеют метки
- Правильная структура заголовков (header: true)
- Кнопки объявлены как кнопки (button: true)
- Поля ввода объявлены как textField

#### ⚠️ Навигация с клавиатуры (веб):
- Нужно добавить поддержку Tab navigation
- Нужно добавить поддержку Enter/Space для активации
- Нужно добавить поддержку Escape для закрытия модальных окон

### 4. Рекомендации по улучшению:

#### А) Универсальные улучшения:

1. **Добавить Semantics во все экраны:**
```dart
// В каждом экране добавить
Semantics(
  label: 'Название экрана',
  header: true, // для заголовков
  child: YourWidget(),
)
```

2. **Использовать AccessibleText везде:**
```dart
// Вместо Text использовать
AccessibleText(
  'Текст',
  semanticsLabel: 'Описание для screen reader',
)
```

3. **Добавить подсказки для кнопок:**
```dart
AccessibleButton(
  label: 'Получить задание',
  hint: 'Генерирует новое задание для развития ребенка',
  onPressed: () => ...,
)
```

#### Б) Специфичные улучшения:

1. **Экран логина/регистрации:**
- Добавить Semantics для всех полей
- Добавить подсказки для форм
- Добавить объявление ошибок через Semantics

2. **Главные экраны:**
- Добавить структуру заголовков
- Добавить landmarks для навигации
- Добавить объявление результатов тестов

3. **Тестовые экраны (уже реализовано частично):**
- Продолжить использование AccessibleText
- Добавить объявление прогресса через Semantics
- Добавить подсказки для вариантов ответов

### 5. Поддержка жестов (мобильные):

Для iOS VoiceOver и Android TalkBack:
- ✅ Жест "swipe right" - следующий элемент
- ✅ Жест "swipe left" - предыдущий элемент
- ✅ Двойное нажатие - активация
- ⚠️ Можно добавить кастомные жесты через Semantics.explicitChildNodes

### 6. Локализация для screen readers:

Все семантические метки должны быть локализованы:
- Использовать `AppLocalizations` для меток
- Казахский (kk) и русский (ru) языки
- Пример: `Semantics(label: l10n.get('loginButton'))`

### 7. Тестирование:

#### Как протестировать:

1. **iOS (VoiceOver):**
   - Settings → Accessibility → VoiceOver → включить
   - Проверить навигацию жестами
   - Проверить объявление всех элементов

2. **Android (TalkBack):**
   - Settings → Accessibility → TalkBack → включить
   - Проверить навигацию жестами
   - Проверить объявление всех элементов

3. **Web (NVDA/JAWS):**
   - Установить NVDA или JAWS
   - Проверить навигацию с Tab
   - Проверить объявление всех элементов

4. **Chrome DevTools:**
   - F12 → Lighthouse → Accessibility
   - Проверить score (должен быть > 90)
   - Исправить найденные проблемы

### 8. Метрики доступности:

#### Должны быть выполнены:
- ✅ Все изображения имеют alt-текст (или декоративные)
- ✅ Все кнопки имеют метки
- ✅ Все формы имеют метки
- ✅ Контрастность >= 4.5:1
- ✅ Навигация с клавиатуры работает
- ✅ Screen readers объявляют все элементы

#### Измерение:
- Lighthouse Accessibility Score: > 90
- WAVE (Web Accessibility Evaluation Tool): 0 ошибок
- axe DevTools: 0 критических проблем

### 9. Чеклист для разработчиков:

При создании нового экрана/виджета:

- [ ] Использовать `AccessibleText` вместо `Text`
- [ ] Использовать `AccessibleButton` для кнопок
- [ ] Использовать `AccessibleTextField` для полей
- [ ] Добавить `Semantics` для контейнеров
- [ ] Добавить метки для всех интерактивных элементов
- [ ] Добавить подсказки (hints) где нужно
- [ ] Проверить контрастность цветов
- [ ] Протестировать с screen reader
- [ ] Протестировать навигацию с клавиатуры (веб)

### 10. Примеры использования:

#### Пример 1: Простая кнопка
```dart
AccessibleButton(
  label: 'Войти',
  hint: 'Войти в аккаунт',
  onPressed: () => _login(),
)
```

#### Пример 2: Карточка с контентом
```dart
AccessibleCard(
  title: 'Результат теста PHQ-9',
  subtitle: 'Умеренная депрессия',
  semanticLabel: 'Результат теста: Умеренная депрессия',
  onTap: () => _showDetails(),
  child: Text('Оценка: 12/27'),
)
```

#### Пример 3: Поле ввода
```dart
AccessibleTextField(
  label: 'Email',
  hint: 'Введите ваш email адрес',
  controller: _emailController,
  keyboardType: TextInputType.emailAddress,
)
```

#### Пример 4: Экран с заголовком
```dart
AccessibleScaffold(
  title: 'Главная страница',
  appBar: AppBar(title: Text('Главная')),
  body: YourContent(),
)
```

## 📋 Следующие шаги:

1. ✅ Создать виджеты доступности (выполнено)
2. ⚠️ Добавить Semantics во все экраны (в процессе)
3. ⚠️ Добавить поддержку навигации с клавиатуры (веб)
4. ⚠️ Протестировать с реальными screen readers
5. ⚠️ Создать руководство для пользователей со слабым зрением

## 🔗 Полезные ссылки:

- WCAG 2.1 Guidelines: https://www.w3.org/WAI/WCAG21/quickref/
- Flutter Semantics: https://api.flutter.dev/flutter/widgets/Semantics-class.html
- Material Accessibility: https://material.io/design/usability/accessibility.html
- VoiceOver Guide: https://support.apple.com/guide/voiceover/
- TalkBack Guide: https://support.google.com/accessibility/android/answer/6283677
