# 📊 Реализация аналитики

## ✅ Что реализовано:

### 1. Firebase Analytics
- ✅ Пакет `firebase_analytics: ^12.1.0` добавлен
- ✅ Инициализация в `main.dart`
- ✅ Сервис `AnalyticsService` создан для логирования событий

### 2. Отслеживаемые события:

#### Основные события:
- `app_install` - установка приложения
- `app_open` - открытие приложения
- `sign_up` - регистрация пользователя
- `login` - вход пользователя

#### События тестов:
- `test_start` - начало теста (phq9, gad7, traffic_light)
- `test_complete` - завершение теста с результатами (score, risk_level)

#### События заданий:
- `serve_and_return_task` - получение задания Serve & Return (возраст ребенка, язык)
- `gemini_task` - генерация задания через Gemini AI

#### События навигации (точки выхода):
- `screen_view` - просмотр экрана
- `screen_exit` - выход с экрана (время на экране)

### 3. Интеграция Amplitude

**Примечание:** Amplitude не добавлен пока, но можно легко интегрировать:

1. Добавить пакет в `pubspec.yaml`:
```yaml
dependencies:
  amplitude_flutter: ^3.10.0  # или последняя версия
```

2. Инициализировать в `main.dart`:
```dart
import 'package:amplitude_flutter/amplitude.dart';

final amplitude = Amplitude.getInstance(instanceName: "anama");
amplitude.init("YOUR_AMPLITUDE_API_KEY");
```

3. Добавить методы в `AnalyticsService`:
```dart
Future<void> logToAmplitude(String eventName, Map<String, dynamic>? params) async {
  await amplitude.logEvent(eventName, eventProperties: params);
}
```

### 4. Дашборд Firebase Analytics

После развертывания аналитика будет доступна в:
- **Firebase Console** → Analytics → Events
- URL: https://console.firebase.google.com/project/anama-app/analytics

### 5. Метрики для отслеживания:

#### Метрики вовлеченности:
- Частота открытия приложения (`app_open`)
- Время на экранах (`screen_exit` с параметром `time_on_screen_seconds`)
- Прохождение тестов (`test_complete`)

#### Метрики точек выхода:
- `screen_exit` - когда пользователь покидает экран
- Параметр `time_on_screen_seconds` - сколько времени провел на экране

#### Метрики конверсии:
- Регистрация (`sign_up`)
- Начало первого теста (`test_start`)
- Завершение теста (`test_complete`)

### 6. Использование:

```dart
import 'package:anama/services/analytics_service.dart';

final analytics = AnalyticsService();

// При открытии приложения
await analytics.logAppOpen();

// При регистрации
await analytics.logSignUp(method: 'email', userId: userId);

// При начале теста
await analytics.logTestStart(testName: 'phq9', userId: userId);

// При завершении теста
await analytics.logTestComplete(
  testName: 'phq9',
  score: 12,
  riskLevel: 'yellow',
  userId: userId,
);

// При получении задания
await analytics.logServeAndReturnTask(
  childAgeMonths: 36,
  languageCode: 'kk',
);

// При просмотре экрана
await analytics.logScreenView(screenName: '/parent/home');
```

## 📋 Следующие шаги:

1. **Добавить события в ключевые точки:**
   - В `parent_home_screen.dart` при получении задания
   - В экранах тестов при начале/завершении
   - В экранах регистрации/входа

2. **Настроить Amplitude (опционально):**
   - Зарегистрироваться на https://amplitude.com
   - Получить API ключ
   - Добавить интеграцию (см. выше)

3. **Создать дашборды:**
   - В Firebase Console настроить события
   - В Amplitude (если добавлен) создать дашборды для фаннелей конверсии

